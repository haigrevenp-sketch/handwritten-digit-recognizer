This directory holds auto-generated images produced by train_model.py:

  training_curves.png   — accuracy & loss over epochs
  confusion_matrix.png  — 10×10 confusion matrix on the test set
  sample_predictions.png — one sample per digit class

Run `python train_model.py` to generate all three files.
